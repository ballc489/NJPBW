# 七彩の塔 — プロジェクト概要 (CLAUDE.md)

このファイルはClaude Code用のプロジェクトメモリです。作業を始める前に必ず読んでください。

## プロジェクトの正体

- ブラウザで動くRPG。単一の `index.html` で完結(HTML/CSS/JSすべて1ファイル)
- バックエンドはSupabase(匿名認証 + Row Level Security + RPC経由の一括保存)
- ホスティングはGitHub Pages、独自ドメイン `tower-of-sevens.com` で公開中
- 現在はCloudflare Accessでアクセス制限をかけた非公開状態(一般公開前の開発段階)

## 題材についての重要な注意(必読)

このゲームは、VTuber事務所「にじさんじ」をゆるやかに題材にした二次創作プロジェクトです。以下は必ず守ってください。

- **公式のロゴ・キャラクターイラストは一切使用しない**。ライバー(キャラクター)は独自の名前・カラー・イラストで表現する
- 現時点でゲーム内のライバー名は `(仮)ライバーA〜E` のようなプレースホルダーのまま。実名反映を進める際は、ANYCOLORの二次創作ガイドラインの範囲(非商用〜運営コスト相当の広告収益程度)を逸脱しないこと
- 「公式作品であるとの誤解を招く表現」は禁止。ロゴ・キャラクターデザインの模倣、公式を騙るような表現は避ける
- 収益化は「運営コストを賄う程度の広告収入」を想定しており、大規模な商用展開は想定していない

## 技術構成

- `index.html` — ゲーム本体。ファイル冒頭で `SUPABASE_URL` / `SUPABASE_ANON_KEY` を直書きしている(publishable keyなので公開して問題ない設計)
- `supabase/` — DBスキーマ・RLSポリシー・RPC関数のSQL。番号順に適用済み(001, 002, 003)。**新しいテーブルやRPCを追加する場合は、次の連番でこのフォルダにSQLファイルを追加すること**
- ゲームの `state` オブジェクトの形は、内部ロジック全体で共有されている前提の設計。持続化層(Supabaseとの読み書き)を変更する際も、`state` の形自体はなるべく変えないこと(過去、window.storage → Supabase移行の際もこの方針で最小限の変更に抑えた)

## ゲームシステムの要点

- パーティ制(最大4人編成)、ダンジョン探索、ボス戦、装備・スキル・ジョブ制
- キャラ作成時に1人だけ好きなライバーを選んで開始し、残りはダンジョン攻略で解放していく設計
- コロシアムタワー(全10F、非同期PvP)。門番の勝敗処理はクライアントの自己申告を一部信用する設計(完全なチート対策はしていないと自覚した上での割り切り)
- グローバルチャットはSupabase Realtimeで実装(ポーリングではない)

## 開発の進め方

- 変更後は必ず一通りブラウザで動作確認する(このプロジェクトには自動テストの仕組みが無い)
- DBスキーマを変更する場合は、既存の `supabase/001〜003` を上書きせず、新しい番号のファイルを追加する形にする
- コミットメッセージは日本語でOK。何を変えたか簡潔に

## 現在の作業フォーカス

ライバーの実データ(名前・カラー・スキル元ネタなど)の反映を進めている最中。
